function isArrayOrNot(a) {
	return Array.isArray(a);
}

function isArrayOrNot(a) {
	return Array.isArray(a);
}

console.log(isArrayOrNot("aee"));
console.log(toString.call("add"))
console.log(toString.call([]))
console.log(toString.call({}))

/**
Array copy : Array.from() or ...
a.concat("b")

JSON.parse(JSON.stringify(nested))
--- only way to deep copy a nested javascript array

https://www.freecodecamp.org/news/how-to-clone-an-array-in-javascript-1d3183468f6a/
**/
